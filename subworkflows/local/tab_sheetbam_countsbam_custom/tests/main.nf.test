// nf-core subworkflows test tab_sheetbam_countsbam_custom_counts
nextflow_workflow {

    name "Test Subworkflow TAB_SHEETBAM_COUNTSBAM_CUSTOM_COUNTS"
    script "../main.nf"
    workflow "TAB_SHEETBAM_COUNTSBAM_CUSTOM_COUNTS"

    tag "subworkflows"
    tag "subworkflows_"
    tag "subworkflows/tab_sheetbam_countsbam_custom_counts"
    tag "samtools"
    tag "samtools/sort"
    tag "samtools/index"


    test("sarscov2 - bam - single_end") {

        when {
            workflow {
                """
                input[0] = [
                    [ id:'test', single_end:false ], // meta map
                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true),
                ]
                input[1] = [
                    [ id:'genome' ],
                    file(params.modules_testdata_base_path + 'genomics/sarscov2/illumina/bam/test.paired_end.sorted.bam', checkIfExists: true),
                ]
                """
            }
        }
        then {
            assert workflow.success
            assertAll(
                { assert snapshot(
                    workflow.out
                ).match() }
            )
        }
    }
}
