// nf-core modules test episegmix/config
nextflow_process {

    name "Test Process EPISEGMIX_CONFIG"
    script "../main.nf"
    process "EPISEGMIX_CONFIG"

    tag "modules"
    tag "modules_episegmix"
    tag "episegmix"
    tag "episegmix/config"

    test("Kidney - EPISEGMIX_CONFIG") {

        when {
            process {
                """
                input[0] = [
                    'kidney_P0_2',
                    [
                        [
                            id: 'kidney_P0',
                            replicate: 1,
                            epigenetic_mark: 'H3K4me3',
                            modality: 'ChIP-seq',
                            paired_end: false,
                            distribution: 'NBI'
                        ]
                    ],
                    file(params.pipelines_testdata_base_path + '/modules/kidney_P0.tab', checkIfExists: true),
                    [ id: 'no_bed_counts' ],
                    [],
                    2
                ]
                """
            }
        }

        then {
            assert process.success
            assertAll(
                { assert snapshot(
                    process.out,
                    process.out.findAll { key, val -> key.startsWith('versions') }
                ).match() }
            )
        }

    }

    test("Kidney - EPISEGMIX_CONFIG - stub") {

        options "-stub"

        when {
            process {
                """
                input[0] = [
                    'kidney_P0_2',
                    [
                        [
                            id: 'kidney_P0',
                            replicate: 1,
                            epigenetic_mark: 'H3K4me3',
                            modality: 'ChIP-seq',
                            paired_end: false,
                            distribution: 'NBI'
                        ]
                    ],
                    file(params.pipelines_testdata_base_path + '/modules/kidney_P0.tab', checkIfExists: true),
                    [ id: 'no_bed_counts' ],
                    [],
                    2
                ]
                """
            }
        }

        then {
            assert process.success
            assertAll(
                { assert snapshot(process.out).match() }
            )
        }

    }

}
